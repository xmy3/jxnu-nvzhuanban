<?xml version="1.0" encoding="utf-8"?>
<!--
    Glance widget provider metadata.

    - minWidth/minHeight: Android 11 and below fallback in dp.
    - updatePeriodMillis=1800000: system fallback refresh every 30 minutes.
      Precise course-boundary refreshes are scheduled by WidgetUpdateScheduler.
    - initialLayout is required and is replaced after the first Glance render.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="220dp"
    android:minHeight="110dp"
    android:updatePeriodMillis="1800000"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:description="@string/widget_description"
    android:previewImage="@mipmap/ic_launcher" />
