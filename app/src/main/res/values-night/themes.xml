<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.Nvzhuanban" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:enforceNavigationBarContrast" tools:targetApi="q">false</item>
        <item name="android:enforceStatusBarContrast" tools:targetApi="q">false</item>
    </style>

    <!-- Splash 主题深色版（女专红在深色模式下保持不变，足够对比） -->
    <style name="Theme.Nvzhuanban.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/brand_nvzhuan_red</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_monochrome</item>
        <item name="postSplashScreenTheme">@style/Theme.Nvzhuanban</item>
    </style>

</resources>
